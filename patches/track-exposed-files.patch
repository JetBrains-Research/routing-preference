diff --git a/src/minisweagent/config/default.yaml b/src/minisweagent/config/default.yaml
index 8f5ebb1a..da548161 100644
--- a/src/minisweagent/config/default.yaml
+++ b/src/minisweagent/config/default.yaml
@@ -25,11 +25,14 @@ agent:
     This workflow should be done step-by-step so that you can iterate on your changes and any possible problems.
 
     1. Analyze the codebase by finding and reading relevant files
-    2. Create a script to reproduce the issue
-    3. Edit the source code to resolve the issue
-    4. Verify your fix works by running your script again
+    2. If the task is a bug fix, write a script to reproduce the issue before fixing it
+       For other tasks (features, refactors, etc.), skip reproduction and go straight to analyzing how to implement the change
+    3. Edit the source code to resolve the task
+    4. Verify your changes: re-run the reproduction script for bugs, or run the relevant tests for other tasks
     5. Test edge cases to ensure your fix is robust
-    6. Submit your changes and finish your work by issuing the following command: `echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT`.
+    6. Write a summary of your changes covering what you changed and why. Your summary must not contain double quotes, backticks, or dollar signs. You can use single quotes or rephrase if needed.
+    7. When your changes are finished, issue the following command: `echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT "<your summary here>"`.
+       Replace `<your summary here>` with the summary you wrote in step 6. The summary must be wrapped in double quotes.
        Do not combine it with any other command. <important>After this command, you cannot continue working on this task.</important>
 
     ## Important Rules
@@ -151,8 +154,9 @@ model:
     Here is general guidance on how to format your response:
 
     Please always provide EXACTLY ONE action in triple backticks, found {{actions|length}} actions.
-    If you want to end the task, please issue the following command: `echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT`
-    without any other command.
+    If you want to end the task, please issue the following command: `echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT "<your summary here>"`.
+    Replace `<your summary here>` with a summary of your changes. The summary must be wrapped in double quotes and must not contain double quotes, backticks, or dollar signs.
+
     Else, please format your response exactly as follows:
 
     <response_example>
@@ -164,4 +168,4 @@ model:
     </response_example>
 
     Note: In rare cases, if you need to reference a similar format in your command, you might have
-    to proceed in two steps, first writing TRIPLEBACKTICKSBASH, then replacing them with ```mswea_bash_command.
\ No newline at end of file
+    to proceed in two steps, first writing TRIPLEBACKTICKSBASH, then replacing them with ```mswea_bash_command.
diff --git a/src/minisweagent/environments/docker.py b/src/minisweagent/environments/docker.py
index 42e65e35..6e51f18e 100644
--- a/src/minisweagent/environments/docker.py
+++ b/src/minisweagent/environments/docker.py
@@ -1,9 +1,11 @@
 import logging
 import os
 import platform
+import re
 import shlex
 import subprocess
 import uuid
+from pathlib import Path
 from typing import Any
 
 from pydantic import BaseModel
@@ -11,6 +13,78 @@ from pydantic import BaseModel
 from minisweagent.exceptions import Submitted
 from minisweagent.utils.serialize import recursive_merge
 
+_READ_COMMANDS = {"cat", "head", "tail", "less", "more", "sed", "awk", "nl", "view", "bat"}
+_PATTERN_COMMANDS = {"sed", "awk", "grep"}
+_PATH_RE = re.compile(r"^[a-zA-Z0-9_./\-]+$")
+
+
+def _normalize_repo_path(path: str, cwd: str = "/workspace") -> str | None:
+    if path in {"", ".", "./", ".."}:
+        return None
+    if path.startswith("/tmp/"):
+        return None
+    if Path(path).is_absolute():
+        workspace = cwd.rstrip("/") + "/"
+        if not path.startswith(workspace):
+            return None
+        path = path.removeprefix(workspace)
+    path = path.removeprefix("./")
+    parts = path.split("/")
+    if any(part in {"", ".", ".."} for part in parts):
+        return None
+    return path
+
+
+def _extract_grep_files(output: str, cwd: str = "/workspace") -> list[str]:
+    paths = []
+    for line in output.splitlines():
+        if ":" not in line:
+            continue
+        raw_path = line.split(":", 1)[0]
+        if raw_path.isdigit():
+            continue
+        if not _PATH_RE.match(raw_path):
+            continue
+        path = _normalize_repo_path(raw_path, cwd)
+        if path and path not in paths:
+            paths.append(path)
+    return paths
+
+
+def _extract_exposed_files(command: str, output: str = "", cwd: str = "/workspace") -> tuple[list[str], list[str]]:
+    """Extract file paths from commands that expose file content to the agent."""
+    paths = []
+    grep_paths = []
+    for segment in re.split(r"(?:&&|\|\||;|\|)", command):
+        try:
+            tokens = shlex.split(segment.strip())
+        except ValueError:
+            continue
+        if not tokens:
+            continue
+        cmd_name = Path(tokens[0]).name
+        if cmd_name == "grep":
+            for path in _extract_grep_files(output, cwd):
+                if path not in grep_paths:
+                    grep_paths.append(path)
+            continue
+        if cmd_name not in _READ_COMMANDS:
+            continue
+        if ">" in segment:
+            continue
+        if cmd_name == "sed" and any(a.startswith("-i") for a in tokens[1:]):
+            continue
+        positionals = [a for a in tokens[1:] if a and not a.startswith("-")]
+        if cmd_name in _PATTERN_COMMANDS and positionals:
+            positionals = positionals[1:]
+        for p in positionals:
+            if not _PATH_RE.match(p):
+                continue
+            path = _normalize_repo_path(p, cwd)
+            if path and path not in paths:
+                paths.append(path)
+    return paths, grep_paths
+
 
 class DockerEnvironmentConfig(BaseModel):
     image: str
@@ -56,6 +130,8 @@ class DockerEnvironment:
         self.logger = logger or logging.getLogger("minisweagent.environment")
         self.container_id: str | None = None
         self.config = config_class(**kwargs)
+        self.exposed_files: list[str] = []
+        self.grep_exposed_files: list[str] = []
         self._start_container()
 
     def get_template_vars(self, **kwargs) -> dict[str, Any]:
@@ -67,7 +143,9 @@ class DockerEnvironment:
                 "config": {
                     "environment": self.config.model_dump(mode="json"),
                     "environment_type": f"{self.__class__.__module__}.{self.__class__.__name__}",
-                }
+                },
+                "exposed_files": list(self.exposed_files),
+                "grep_exposed_files": list(self.grep_exposed_files),
             }
         }
 
@@ -134,14 +212,26 @@ class DockerEnvironment:
                 "exception_info": f"An error occurred while executing the command: {e}",
                 "extra": {"exception_type": type(e).__name__, "exception": str(e)},
             }
+        if output.get("returncode") == 0:
+            exposed_files, grep_exposed_files = _extract_exposed_files(command, output.get("output", ""), cwd)
+            for path in exposed_files:
+                if path not in self.exposed_files:
+                    self.exposed_files.append(path)
+            for path in grep_exposed_files:
+                if path not in self.grep_exposed_files:
+                    self.grep_exposed_files.append(path)
         self._check_finished(output)
         return output
 
     def _check_finished(self, output: dict):
         """Raises Submitted if the output indicates task completion."""
+        marker = "COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT"
         lines = output.get("output", "").lstrip().splitlines(keepends=True)
-        if lines and lines[0].strip() == "COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT" and output["returncode"] == 0:
-            submission = "".join(lines[1:])
+        first_line = lines[0].strip() if lines else ""
+        if lines and (first_line == marker or first_line.startswith(marker + " ")) and output["returncode"] == 0:
+            first_line_submission = first_line[len(marker) :].lstrip()
+            rest_submission = "".join(lines[1:])
+            submission = (first_line_submission + "\n" + rest_submission).strip()
             raise Submitted(
                 {
                     "role": "exit",
diff --git a/src/minisweagent/environments/extra/bubblewrap.py b/src/minisweagent/environments/extra/bubblewrap.py
index bd6f9183..dd556548 100644
--- a/src/minisweagent/environments/extra/bubblewrap.py
+++ b/src/minisweagent/environments/extra/bubblewrap.py
@@ -118,9 +118,13 @@ class BubblewrapEnvironment:
 
     def _check_finished(self, output: dict):
         """Raises Submitted if the output indicates task completion."""
+        marker = "COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT"
         lines = output.get("output", "").lstrip().splitlines(keepends=True)
-        if lines and lines[0].strip() == "COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT" and output["returncode"] == 0:
-            submission = "".join(lines[1:])
+        first_line = lines[0].strip() if lines else ""
+        if lines and (first_line == marker or first_line.startswith(marker + " ")) and output["returncode"] == 0:
+            first_line_submission = first_line[len(marker) :].lstrip()
+            rest_submission = "".join(lines[1:])
+            submission = (first_line_submission + "\n" + rest_submission).strip()
             raise Submitted(
                 {
                     "role": "exit",
diff --git a/src/minisweagent/environments/extra/contree.py b/src/minisweagent/environments/extra/contree.py
index 1d6a8c9d..7758cc3b 100644
--- a/src/minisweagent/environments/extra/contree.py
+++ b/src/minisweagent/environments/extra/contree.py
@@ -123,9 +123,13 @@ class ContreeEnvironment(Environment):
 
     def _check_finished(self, output: dict):
         """Raises Submitted if the output indicates task completion."""
+        marker = "COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT"
         lines = output.get("output", "").lstrip().splitlines(keepends=True)
-        if lines and lines[0].strip() == "COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT" and output["returncode"] == 0:
-            submission = "".join(lines[1:])
+        first_line = lines[0].strip() if lines else ""
+        if lines and (first_line == marker or first_line.startswith(marker + " ")) and output["returncode"] == 0:
+            first_line_submission = first_line[len(marker) :].lstrip()
+            rest_submission = "".join(lines[1:])
+            submission = (first_line_submission + "\n" + rest_submission).strip()
             raise Submitted(
                 {
                     "role": "exit",
diff --git a/src/minisweagent/environments/extra/swerex_docker.py b/src/minisweagent/environments/extra/swerex_docker.py
index 9380d2f6..0265458f 100644
--- a/src/minisweagent/environments/extra/swerex_docker.py
+++ b/src/minisweagent/environments/extra/swerex_docker.py
@@ -55,9 +55,13 @@ class SwerexDockerEnvironment:
 
     def _check_finished(self, output: dict):
         """Raises Submitted if the output indicates task completion."""
+        marker = "COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT"
         lines = output.get("output", "").lstrip().splitlines(keepends=True)
-        if lines and lines[0].strip() == "COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT" and output["returncode"] == 0:
-            submission = "".join(lines[1:])
+        first_line = lines[0].strip() if lines else ""
+        if lines and (first_line == marker or first_line.startswith(marker + " ")) and output["returncode"] == 0:
+            first_line_submission = first_line[len(marker) :].lstrip()
+            rest_submission = "".join(lines[1:])
+            submission = (first_line_submission + "\n" + rest_submission).strip()
             raise Submitted(
                 {
                     "role": "exit",
diff --git a/src/minisweagent/environments/extra/swerex_modal.py b/src/minisweagent/environments/extra/swerex_modal.py
index 298b0039..4cf87c57 100644
--- a/src/minisweagent/environments/extra/swerex_modal.py
+++ b/src/minisweagent/environments/extra/swerex_modal.py
@@ -90,9 +90,13 @@ class SwerexModalEnvironment:
 
     def _check_finished(self, output: dict):
         """Raises Submitted if the output indicates task completion."""
+        marker = "COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT"
         lines = output.get("output", "").lstrip().splitlines(keepends=True)
-        if lines and lines[0].strip() == "COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT" and output["returncode"] == 0:
-            submission = "".join(lines[1:])
+        first_line = lines[0].strip() if lines else ""
+        if lines and (first_line == marker or first_line.startswith(marker + " ")) and output["returncode"] == 0:
+            first_line_submission = first_line[len(marker) :].lstrip()
+            rest_submission = "".join(lines[1:])
+            submission = (first_line_submission + "\n" + rest_submission).strip()
             raise Submitted(
                 {
                     "role": "exit",
diff --git a/src/minisweagent/environments/local.py b/src/minisweagent/environments/local.py
index b22db383..70201f5e 100644
--- a/src/minisweagent/environments/local.py
+++ b/src/minisweagent/environments/local.py
@@ -1,6 +1,9 @@
 import os
 import platform
+import re
+import shlex
 import subprocess
+from pathlib import Path
 from typing import Any
 
 from pydantic import BaseModel
@@ -8,6 +11,85 @@ from pydantic import BaseModel
 from minisweagent.exceptions import Submitted
 from minisweagent.utils.serialize import recursive_merge
 
+_READ_COMMANDS = {"cat", "head", "tail", "less", "more", "sed", "awk", "nl", "view", "bat"}
+_PATTERN_COMMANDS = {"sed", "awk", "grep"}
+_PATH_RE = re.compile(r"^[a-zA-Z0-9_./\-]+$")
+
+
+def _normalize_repo_path(path: str, cwd: str = "") -> str | None:
+    if path in {"", ".", "./", ".."}:
+        return None
+    if path.startswith("/tmp/"):
+        return None
+
+    if cwd and Path(path).is_absolute():
+        try:
+            rel_path = os.path.relpath(path, cwd)
+        except ValueError:
+            return None
+        if rel_path.startswith(".."):
+            return None
+        path = rel_path
+
+    path = path.removeprefix("./")
+    parts = path.split("/")
+    if any(part in {"", ".", ".."} for part in parts):
+        return None
+    if cwd and not (Path(cwd) / path).is_file():
+        return None
+    return path
+
+
+def _extract_grep_files(output: str, cwd: str = "") -> list[str]:
+    paths = []
+    for line in output.splitlines():
+        if ":" not in line:
+            continue
+        raw_path = line.split(":", 1)[0]
+        if raw_path.isdigit():
+            continue
+        if not _PATH_RE.match(raw_path):
+            continue
+        path = _normalize_repo_path(raw_path, cwd)
+        if path and path not in paths:
+            paths.append(path)
+    return paths
+
+
+def _extract_exposed_files(command: str, output: str = "", cwd: str = "") -> tuple[list[str], list[str]]:
+    """Extract file paths from commands that expose file content to the agent."""
+    paths = []
+    grep_paths = []
+    for segment in re.split(r"(?:&&|\|\||;|\|)", command):
+        try:
+            tokens = shlex.split(segment.strip())
+        except ValueError:
+            continue
+        if not tokens:
+            continue
+        cmd_name = Path(tokens[0]).name
+        if cmd_name == "grep":
+            for path in _extract_grep_files(output, cwd):
+                if path not in grep_paths:
+                    grep_paths.append(path)
+            continue
+        if cmd_name not in _READ_COMMANDS:
+            continue
+        if ">" in segment:
+            continue
+        if cmd_name == "sed" and any(a.startswith("-i") for a in tokens[1:]):
+            continue
+        positionals = [a for a in tokens[1:] if a and not a.startswith("-")]
+        if cmd_name in _PATTERN_COMMANDS and positionals:
+            positionals = positionals[1:]
+        for p in positionals:
+            if not _PATH_RE.match(p):
+                continue
+            path = _normalize_repo_path(p, cwd)
+            if path and path not in paths:
+                paths.append(path)
+    return paths, grep_paths
+
 
 class LocalEnvironmentConfig(BaseModel):
     cwd: str = ""
@@ -19,6 +101,8 @@ class LocalEnvironment:
     def __init__(self, *, config_class: type = LocalEnvironmentConfig, **kwargs):
         """This class executes bash commands directly on the local machine."""
         self.config = config_class(**kwargs)
+        self.exposed_files: list[str] = []
+        self.grep_exposed_files: list[str] = []
 
     def execute(self, action: dict, cwd: str = "", *, timeout: int | None = None) -> dict[str, Any]:
         """Execute a command in the local environment and return the result as a dict."""
@@ -49,14 +133,26 @@ class LocalEnvironment:
                 "exception_info": f"An error occurred while executing the command: {e}",
                 "extra": {"exception_type": type(e).__name__, "exception": str(e)},
             }
+        if output.get("returncode") == 0:
+            exposed_files, grep_exposed_files = _extract_exposed_files(command, output.get("output", ""), cwd)
+            for path in exposed_files:
+                if path not in self.exposed_files:
+                    self.exposed_files.append(path)
+            for path in grep_exposed_files:
+                if path not in self.grep_exposed_files:
+                    self.grep_exposed_files.append(path)
         self._check_finished(output)
         return output
 
     def _check_finished(self, output: dict):
         """Raises Submitted if the output indicates task completion."""
+        marker = "COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT"
         lines = output.get("output", "").lstrip().splitlines(keepends=True)
-        if lines and lines[0].strip() == "COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT" and output["returncode"] == 0:
-            submission = "".join(lines[1:])
+        first_line = lines[0].strip() if lines else ""
+        if lines and (first_line == marker or first_line.startswith(marker + " ")) and output["returncode"] == 0:
+            first_line_submission = first_line[len(marker) :].lstrip()
+            rest_submission = "".join(lines[1:])
+            submission = (first_line_submission + "\n" + rest_submission).strip()
             raise Submitted(
                 {
                     "role": "exit",
@@ -74,6 +170,8 @@ class LocalEnvironment:
                 "config": {
                     "environment": self.config.model_dump(mode="json"),
                     "environment_type": f"{self.__class__.__module__}.{self.__class__.__name__}",
-                }
+                },
+                "exposed_files": list(self.exposed_files),
+                "grep_exposed_files": list(self.grep_exposed_files),
             }
         }
diff --git a/src/minisweagent/environments/singularity.py b/src/minisweagent/environments/singularity.py
index a06e952c..60744d03 100644
--- a/src/minisweagent/environments/singularity.py
+++ b/src/minisweagent/environments/singularity.py
@@ -120,9 +120,13 @@ class SingularityEnvironment:
 
     def _check_finished(self, output: dict):
         """Raises Submitted if the output indicates task completion."""
+        marker = "COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT"
         lines = output.get("output", "").lstrip().splitlines(keepends=True)
-        if lines and lines[0].strip() == "COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT" and output["returncode"] == 0:
-            submission = "".join(lines[1:])
+        first_line = lines[0].strip() if lines else ""
+        if lines and (first_line == marker or first_line.startswith(marker + " ")) and output["returncode"] == 0:
+            first_line_submission = first_line[len(marker) :].lstrip()
+            rest_submission = "".join(lines[1:])
+            submission = (first_line_submission + "\n" + rest_submission).strip()
             raise Submitted(
                 {
                     "role": "exit",
