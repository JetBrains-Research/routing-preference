# Getting Started

## Setup

```bash
git clone --recurse-submodules https://github.com/YOUR_ORG/routing-preference.git
cd routing-preference
make setup
```

This installs OpenCode (from submodule) and LiteLLM.

## Configure API Keys

```bash
cp .env.example .env
# Edit .env with your API keys
```
